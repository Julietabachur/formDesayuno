class Tortas {
    constructor (nombre , precio , descripcion, cantidad) {
       this.nombre = nombre,
       this.precio = precio,
       this.cantidad = cantidad, 
       this.descripcion = descripcion
    }
}

class Salado {
    constructor (nombre , precio , descripcion, cantidad) {
       this.nombre = nombre,
       this.precio = precio,
       this.cantidad = cantidad,
       this.descripcion = descripcion
    }
}


class Bebida {
    constructor (nombre , precio , descripcion, cantidad) {
       this.nombre = nombre,
       this.precio = precio,
       this.cantidad = cantidad,
       this.descripcion = descripcion
    }
}

class Regalo {
    constructor (nombre , precio , descripcion, cantidad) {
       this.nombre = nombre,
       this.precio = precio,
       this.cantidad = cantidad,
       this.descripcion = descripcion
    }
}

